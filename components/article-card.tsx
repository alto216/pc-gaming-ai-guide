import Link from "next/link";
import type { Article } from "@/lib/types";
import { categoryPath } from "@/lib/site";
export function ArticleCard({ article, featured = false }: { article: Article; featured?: boolean }) { return <article className={`article-card${featured ? " featured-card" : ""}`}><div className="card-visual"><span className="visual-chip">{article.category}</span><span className="visual-mark" aria-hidden="true">{article.category === "GPU" ? "▧" : article.category === "ローカルAI" ? "◉" : article.category === "ゲーミングPC" ? "⌁" : "▱"}</span><span className="visual-caption">PC NOTE / FIELD GUIDE</span></div><div className="card-body"><div className="card-meta"><Link href={`/${categoryPath[article.category]}/`}>{article.category}</Link><time dateTime={article.date}>{article.date}</time></div><h3><Link href={`/articles/${article.slug}/`}>{article.title}</Link></h3><p>{article.description}</p><Link className="text-link" href={`/articles/${article.slug}/`}>記事を読む <span aria-hidden="true">↗</span></Link></div></article>; }
